export class GameManager {
    private Game : Game[] 

    constructor() {
        this.Game = [];
    }

    addUser(socket : WebSocket) {

    }

    removeUser(socket : WebSocket ){
        
    }
}